// Default news placeholder image
// Used as fallback when external image URLs fail to load
import globalNewsImage from '../assets/global-news.png';

export const DEFAULT_NEWS_IMAGE = globalNewsImage;

export const NEWS_PLACEHOLDER = DEFAULT_NEWS_IMAGE;
